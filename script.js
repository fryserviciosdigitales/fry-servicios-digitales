const menuBtn=document.querySelector('.menu-btn');const menu=document.querySelector('#menu');menuBtn.addEventListener('click',()=>{const open=menu.classList.toggle('open');menuBtn.setAttribute('aria-expanded',open)});document.querySelectorAll('#menu a').forEach(a=>a.addEventListener('click',()=>{menu.classList.remove('open');menuBtn.setAttribute('aria-expanded','false')}));document.querySelectorAll('.filter').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.card[data-category]').forEach(card=>{card.hidden=!(f==='all'||card.dataset.category===f)})}));document.querySelector('#contact-form').addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.currentTarget);const subject=encodeURIComponent('Solicitud de información - '+(data.get('empresa')||data.get('nombre')));const body=encodeURIComponent(`Hola Fry Servicios Digitales,\n\nQuiero solicitar información.\n\nNombre: ${data.get('nombre')}\nEmpresa: ${data.get('empresa')}\nTeléfono: ${data.get('telefono')}\nCorreo: ${data.get('correo')}\nServicio: ${data.get('servicio')}\n\nNecesidad:\n${data.get('mensaje')}\n\nGracias.`);window.location.href=`mailto:fryserviciosdigitales@gmail.com?subject=${subject}&body=${body}`});if('serviceWorker'in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('./service-worker.js').catch(()=>{}))}